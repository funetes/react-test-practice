import React from 'react';
import renderer from 'react-test-renderer';
import Foo from '../Foo';

describe('Counter', () => {
  let component = null;

  it('renders correctly', () => {
    // component = renderer.create(<Foo />);
    // console.log(component);
    // console.log('-----');
    // console.log('toJSON()', component.toJSON());
    // console.log('toTree()', component.toTree());
  });

  // it('matches snapshot', () => {
  //   const tree = component.toJSON();
  //   expect(tree).toMatchSnapshot();
  // })
});
